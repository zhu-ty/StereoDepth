[ImageRectify]

dir = .

Master = CICAU1641057_00_00000.jpg ;_master.jpg will always be corresponding to MasterImg
Slave = CICAU1641089_01_00000.jpg ; correspond to _slave.jpg

intrinsics = intrinsics.yml
extrinsics = extrinsics.yml

inv = false ; inv param will specify how we rectify the img, but will not affect the master/slave order
resize = true